package Azure::Network::ListAllNetworkSecurityGroupsResult;
  use Moose;

  has nextLink => (is => 'ro', isa => 'Str'  );
  has value => (is => 'ro', isa => 'ArrayRef[Azure::Network::NetworkSecurityGroup]'  );

1;
